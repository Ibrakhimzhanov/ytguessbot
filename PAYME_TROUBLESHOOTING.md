# Решение проблемы с платежами Payme

## Проблема
Когда пользователь нажимает "Men to'ladim", появляется сообщение "To'lovingiz qayta ishlanmoqda" и при нажатии "Yana tekshirish" ничего не меняется.

## Причина
Payme использует **Merchant API** через webhook. После оплаты пользователем, Payme должен отправить запрос на ваш сервер с методом `PerformTransaction`, чтобы подтвердить платеж. Если этот webhook не приходит, статус платежа остается `PENDING` в вашей БД.

## Workflow Payme Merchant API

1. **Пользователь создает заказ** → создается запись в БД со статусом `PENDING`
2. **Пользователь переходит по ссылке Payme Checkout** → оплачивает на странице Payme
3. **Payme вызывает ваш webhook:**
   - `CheckPerformTransaction` - проверяет, можно ли создать транзакцию
   - `CreateTransaction` - создает транзакцию, сохраняет `paymeId`
   - `PerformTransaction` - **подтверждает оплату**, меняет статус на `PAID`
4. **Пользователь получает уведомление** в Telegram о подтверждении

## Что проверить

### 1. Правильно ли настроен Billing URL в личном кабинете Payme

В личном кабинете Payme (merchant.payme.uz) должен быть указан:
```
https://ytacademy.uz/api/payme/billing
```

**Проверьте:**
- URL должен быть доступен извне (не localhost)
- Сервер должен быть запущен и доступен по этому адресу
- SSL сертификат должен быть валидным (Payme требует HTTPS)

### 2. Проверить логи сервера

Посмотрите логи приложения, ищите строки:
```
🔵 Payme Billing Request received
📋 Payme Request: Method: PerformTransaction
```

Если таких логов нет - webhook от Payme не доходит до сервера.

### 3. Проверить авторизацию

В `.env` файле:
```env
PAYME_X_AUTH='Paycom:34Gsv3pFKZsGXYe2oyAyuzq%b0dxz?UGsg&e'
```

- Логин всегда `Paycom`
- Пароль - это ваш KEY из личного кабинета Payme (раздел "Тестирование" или "Кассы")

### 4. Проверить режим работы

В `.env`:
```env
PAYME_IS_TEST=false  # false = продакшн, true = тестовый режим
```

Убедитесь что:
- Режим соответствует вашей кассе в Payme
- KEY в `PAYME_X_AUTH` соответствует режиму (тестовый/продакшн)

## Новые админские команды

### `/admin_check_payment <order_number>`
Проверяет информацию о платеже для диагностики:
```
/admin_check_payment 123
```

Показывает:
- Текущий статус платежа
- Есть ли `paymeId` (транзакция создана в Payme или нет)
- Время с момента создания
- Информацию о пользователе

### `/admin_confirm_payment <order_number>`
Вручную подтверждает платеж, если webhook не сработал:
```
/admin_confirm_payment 123
```

**Используйте только если:**
- Пользователь реально оплатил (проверьте в личном кабинете Payme)
- Webhook не сработал по техническим причинам
- Прошло более 5 минут с момента оплаты

## Как пользователь может проверить статус

Когда пользователь нажимает "Yana tekshirish", бот теперь показывает:

**Если есть `paymeId` (транзакция создана):**
```
⏳ To'lovingiz qayta ishlanmoqda

✅ To'lov Payme tizimida ro'yxatdan o'tdi
⏱️ Tasdiqlash kutilmoqda...
```
→ Значит пользователь оплатил, ждем webhook от Payme

**Если нет `paymeId`:**
```
⏳ To'lov kutilmoqda

❓ To'lov jarayoni:
1. "💳 To'lovga o'tish" tugmasini bosing
2. Payme sahifasida to'lovni amalga oshiring
```
→ Значит пользователь еще не завершил оплату на странице Payme

## Что делать если webhook не работает

### Вариант 1: Проверить доступность webhook URL

Протестируйте напрямую:
```bash
curl -X POST https://ytacademy.uz/api/payme/billing \
  -H "Authorization: Basic $(echo -n 'Paycom:34Gsv3pFKZsGXYe2oyAyuzq%b0dxz?UGsg&e' | base64)" \
  -H "Content-Type: application/json" \
  -d '{"id":1,"method":"CheckPerformTransaction","params":{"account":{"order_id":"1"},"amount":250000000}}'
```

Должен вернуть JSON ответ (не 404, не 500).

### Вариант 2: Использовать ngrok для тестирования (если сервер недоступен)

```bash
ngrok http 3000
```

Скопируйте URL (например, `https://abc123.ngrok.io`) и укажите в Payme:
```
https://abc123.ngrok.io/api/payme/billing
```

### Вариант 3: Вручную подтверждать платежи (временное решение)

Когда пользователь жалуется:
1. Узнайте номер заказа (orderNumber)
2. Проверьте в личном кабинете Payme что оплата прошла
3. Выполните: `/admin_confirm_payment <order_number>`

## Контакты поддержки Payme

Если проблема не решается:
- Telegram: @paymeuzbot
- Email: support@payme.uz
- Телефон: +998 78 150 01 11

## Логирование для диагностики

Все проверки платежей теперь логируются:
```
🔍 Payment check: orderNumber=123, status=PENDING, paymeId=abc123, elapsed=5min
```

Следите за этими логами в консоли сервера.
